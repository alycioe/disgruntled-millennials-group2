<!DOCTYPE html>

<html lang="en">

<head>
    <meta charset="UTF-8">

    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="stylesheet" href="./../public/css/reset.css">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css"> 

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">

    <link rel="stylesheet" href="./../public/css/style.css">

    <title>Login</title>

</head>

<body>
    <div id="login-form">
        <h2>Login</h2>
        <form id="login-form">
            <label for="login-identifier">Email or Username:</label>
            <input type="text" name="login-identifier" id="login-identifier" required>

            <label for="login-password">Password:</label>
            <input type="password" id="login-password" name="login-password" required>

            <button type="submit">Login</button>
        </form>

    </section>

    <script defer src="https://use.fontawesome.com/releases/v5.3.1/js/all.js"></script>

    <script src="./../public/js/login.js"></script>

<!-- <div id="signup-form" style="display: none;">
        <h2>Sign Up</h2>
        <form id="signup-form">
            <label for="signup-email">Email:</label>
            <input type="text" name="signup-email" id="signup-email" required>

            <label for="signup-username">Username:</label>
            <input type="text" name="signup-username" id="signup-username" required>

            <label for="signup-password">Password:</label>
            <input type="password" name="signup-password" id="signup-password" required>

            <label for="animal-choice">Choose Your Animal</label>
            <select name="animal-choice" id="animal-choice">
                <option value="dog">Dog</option>
                <option value="cat">Cat</option>
            </select>

            <button type="submit">Signup</button>
        </form>
    </div>
     <script id="login-template" type="text/x-handlebars-template">
        <h2>Login</h2>
        <form id="login-form">
            <label for="login-identifier">Email or Username:</label>
            <input type="text" id="login-identifier" name="login-identifier" required>

            <button type="submit">Login</button>
        </form>
    </script>

<script id="signup-template" type="text/x-handlebars-template">
    <h2>SignUp</h2>
    <form id="signup-form">
        <label for="signup-email">Email:</label>
        <input type="email" id="signup-email" name="signup-email" required>

        <label for="signup-username">Username:</label>
        <input type="text" name="signup-username" id="singup-username" required>

        <label for="signup-password">Password:</label>
        <input type="password" name="signup-password" id="signup-password" required>

        <label for="animal-choice">Choose Your Animal</label>
        <select name="animal-choice" id="animal-choice">
            <option value="dog">Dog</option>
            <option value="cat">Cat</option>
        </select>

        <button type="submit">SignUp</button>
    </form>
</script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/handlebars.js/4.7.7/handlebars.min.js"></script>
    <script>
        var loginTemplateSource = document.getElementById("login-template").innerHTML;
        var loginTemplate = Handlebars.compile(loginTemplateSource);

        var signupTemplateSource = document.getElementById("signup-template").innerHTML;
        var signupTemplate = Handlebars.compile(signupTemplateSource);

        document.getElementById("toggle-form").addEventListener("click", function() {
            var loginForm = document.getElementById("login-form");
            var signupForm = document.getElementById("signup-form");

            if (loginForm.style.display === "block") {
                loginForm.style.display = "none";
                signupForm.style.display = "block";
                document.getElementById("toggle-form").textContent = "Switch to Login";
            } else {
                loginForm.style.display = "block";
                signupForm.style.display = "none";
                document.getElementById("toggle-form").textContent = "Switch to Sign Up";
            }
        });

        document.getElementById("login-form").addEventListener("submit", function(event) {
            event.preventDefault();
            var loginIdentifier = document.getElementById("login-identifier").value;
            var loginPassword = document.getElementById("login-password").value;

            var loginData = {
                loginIdentifier: loginIdentifier,
                loginPassword: loginPassword
            };

            fetch('/login', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(loginData)
            })
            .then(response => response.json())
            .then(data => {
                alert(data.message);
            })
            .catch(error => {
                console.error('Error:', error);
            });

        });

        function isValidEmail(email) {
            // Email logic goes here, return true if valid email
        }
    // Add login logic
        document.getElementById("signup-form").addEventListener("submit", function(event) {
            event.preventDefault();
            var signupEmail = document.getElementById("signup-email").value;
            var signupUsername = document.getElementById("signup-username").value;
            var signupPassword = document.getElementById("signup-password").value;
            var animalChoice = document.getElementById("animal-choice").value;
            // Add signup logic
</body>

</html>